class AppDurations {
  static const Duration typingAnimationStep = Duration(milliseconds: 400);
  static const Duration typingIndicatorFade = Duration(milliseconds: 200);
  static const Duration weatherFetchDelay = Duration(seconds: 5);
}
