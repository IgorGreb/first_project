class AppDurations {
  static const Duration typingAnimationStep = Duration(milliseconds: 400);
  static const Duration typingIndicatorFade = Duration(milliseconds: 200);
  static const Duration weatherFetchDelay = Duration(seconds: 5);
  static const Duration multiStageInitDelay = Duration(seconds: 2);
  static const Duration multiStageRequestDelay = Duration(seconds: 1);
  static const Duration multiStageResponseDelay = Duration(seconds: 3);
}
